git -> add -> commit -> push
vùng local, working directory, staging area, respository
JavaScript basic:
- biến: let const
- console.log("nội dung cần log");
- toán tử so sánh > < =; ===; !==
- toán tử logic: && ( AND), || ( OR)
- toán tử 1 ngôi: 
   + x++; toán tử nằm ở phía sau - trả về trước, tăng sau
   + X--; toán tử nằm ở phía sau - trả về trước, tăng sau
   + ++x;toán tử nằm ở phía trước - tăng trước, trả về sau
   + -- x: toán tử nằm ở phía trước - tăng trước, trả về sau